'use strict';
/*var Firebase = require('firebase');*/

/**
 * Get clients
 */
exports.clients = function(req, res) {

    /**
    var clientsRef = new Firebase('https://greetings-db.firebaseio.com/clients');

    clientsRef.on('value', function(snapshot) {
        res.send(snapshot.val());
    });
  */

};
